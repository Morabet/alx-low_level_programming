#include "main.h"
#include <string.h>
/**
  * _strcpy - entry point
  * @dest: param
  * @src: param
  *
  * Return: char
  */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
